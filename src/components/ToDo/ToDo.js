import React from 'react';
import PropTypes from 'prop-types';
import './ToDo.css';

const ToDo = () => {
  return (
    <div>
      ToDo Component
    </div>
  );
};

ToDo.propTypes = {};

ToDo.defaultProps = {};

export default ToDo;
